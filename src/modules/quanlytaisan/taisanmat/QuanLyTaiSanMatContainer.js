import { compose, lifecycle } from 'recompose';

import QuanLyTaiSanMatScreen from '../QuanLyTaiSan';

export default compose(lifecycle({
  componentDidMount() {
    
  },
}),)(QuanLyTaiSanMatScreen);
